<h1>Edit Passport</h1>

<div class="container my-4">
  <div class="container my-2">
    <%= render 'form', passport: @passport %>
  </div>

  <div class="container">
    <%= link_to 'Cancel', passport_path(@passport), class:"btn btn-primary"%>
  </div>
</div>
